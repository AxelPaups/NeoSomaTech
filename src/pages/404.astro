---
import NotFoundView from "../views/NotFoundView.astro";
import { localeFromPath } from "../i18n";

// Cloudflare sert cette page pour toute adresse inconnue : la langue se déduit du préfixe de l'URL.
---

<NotFoundView locale={localeFromPath(Astro.url.pathname)} />
